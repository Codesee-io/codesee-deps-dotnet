name: codesee-deps-dotnet package

on: [push]

env:
  NET_VERSION: net7.0
  NET_ACTION_VERSION: '7.x'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET Core SDK ${{ env.NET_ACTION_VERSION }}
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ env.NET_ACTION_VERSION }}
          dotnet-quality: 'ga'
      - name: Publish
        run: dotnet publish --framework ${{ env.NET_VERSION }} --runtime linux-x64 --self-contained false --configuration Release -o output
      - name: Run Tests
        run: dotnet test ./output/TestDependencyReading.dll

